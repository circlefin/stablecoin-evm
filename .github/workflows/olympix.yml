name: "Olympix Scan"
on:
  pull_request:
    branches: [ "deploy" ]
  workflow_dispatch:
  schedule:
    - cron: '31 14 * * 1' # Every Monday 2:31PM UTC

jobs:
  run_olympix:
    if: ${{ github.repository_owner == 'circlefin' }}
    uses: circlefin/security-seceng-templates/.github/workflows/olympix_scan_yarn.yml@v1
